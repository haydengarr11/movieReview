import {useAppContext} from "../context/appContext"

const PageBtnContainer = (page) => {
  const {
    moviePage,
    numOfMoviePages,
    showPage,
    ownMoviePage,
    numOfOwnMoviePages,
    numOfShowPages,
    ownShowPage,
    numOfOwnShowPages,
  } = useAppContext()
}

export default PageBtnContainer
